{
  "description": "An L-shaped bracket with 100mm long arms, 20mm wide, 5mm thick, and two 6mm diameter mounting holes on each arm.",
  "code": "import cadquery as cq\n\nresult = (\n    cq.Workplane(\"XY\")\n    .box(100, 20, 5)  # Horizontal arm\n    .faces(\"<Y\")\n    .workplane()\n    .box(20, 20, 5)  # Vertical arm\n    .edges(\"<Z and >X\")\n    .fillet(5)  # Rounded corner for strength\n    .faces(\">Z\")\n    .workplane()\n    .rect(90, 10, forConstruction=True)\n    .vertices()\n    .hole(6)\n    .faces(\"<Y\")\n    .workplane()\n    .rect(10, 10, forConstruction=True)\n    .vertices()\n    .hole(6)\n)",
  "id": "synthetic_1",
  "source": "synthetic",
  "category": "brackets",
  "prompt": "An L-shaped bracket with 100mm long arms, 20mm wide, 5mm thick, and two 6mm diameter mounting holes on each arm."
}